/**
 * Placeholder entry for @juxbly/playground (stage 0-2 skeleton).
 *
 * apps/extension becomes the WXT entry in stage 0-3.
 * apps/playground hosts the benchmark runner in stage 2-3.
 */
export const appId = '@juxbly/playground' as const
